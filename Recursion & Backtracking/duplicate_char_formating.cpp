#include <iostream>
using namespace std;

void duplicate(char *s, int len) {
	//base
	if(l==0) {
		return;
	}
	//rec
	if

}

int main() {

	#ifndef ONLINE_JUDGE
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
	#endif

	char s[100];
	cin>>s;
	int l=s.length();
	sort(s,s+l);
	duplicate(s,l);


	return 0;
}